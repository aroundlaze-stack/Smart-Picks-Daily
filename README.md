# Smart Picks Daily

Modern tech review & affiliate site — React 19 · Vite 7 · TypeScript · Tailwind CSS v4 · React Three Fiber.

## Quick Start

```bash
npm install
npm run dev
```

Open **http://localhost:5173**

## Scripts

| Command             | Description                      |
|---------------------|----------------------------------|
| `npm run dev`       | Dev server at localhost:5173     |
| `npm run build`     | Production build → `dist/`       |
| `npm run preview`   | Preview the production build     |
| `npm run typecheck` | TypeScript type-check (no emit)  |

## What's New (This Phase)

- **AI Intelligence Core** — R3F hero with icosahedron, neural torus rings, orbiting particles, mouse parallax, WebGL-safe CSS fallback
- **Cockpit Theme Switch** — Aircraft-style dark/light toggle in navbar (spring animation, LED indicator, localStorage)
- **Feature Strip** — Five animated glass pill cards below hero CTAs
- **Micro-interactions** — Framer Motion on every button, card, stat, and section entrance

## Tech Stack

React 19 · TypeScript · Vite 7 · Tailwind CSS v4 · Framer Motion 12  
React Three Fiber 9 · three.js 0.178 · @react-three/drei 10  
Wouter · @tanstack/react-query · Radix UI · Lucide icons
